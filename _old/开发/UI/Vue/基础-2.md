---
layout: post
title: 基础-2
subtitle:
date:       2021-04-02 10:20:26
categories: [Vue]
tags: [Vue,基础-2]
---



# 一、概览
开发版：
`https://cn.vuejs.org/js/vue.js`
生产版：
`https://cn.vuejs.org/js/vue.min.js`

(dist)[https://cdn.jsdelivr.net/npm/vue@2.6.14/dist/]

# 1. 版本

## 1.1 UMD	
## 1.2 CommonJS	
配合老的打包工具比如 `Browserify` 或 `webpack 1`。这些打包工具的默认文件 (pkg.main) 是只包含运行时的 CommonJS 版本 (vue.runtime.common.js)。
## 1.3 ES Module (基于构建工具使用)	
从 2.6 开始为打包工具提供的 ESM：
为诸如 `webpack 2` 或 `Rollup` 提供的现代打包工具。
ESM 格式被设计为可以被静态分析，所以打包工具可以利用这一点来进行“tree-shaking”并将用不到的代码排除出最终的包。为这些打包工具提供的默认文件 (pkg.module) 是只有运行时的 ES Module 构建 (vue.runtime.esm.js)。
## 1.4 ES Module (直接用于浏览器)


完整版：同时包含编译器和运行时的版本。
编译器：用来将模板字符串编译成为 JavaScript 渲染函数的代码。
运行时：用来创建 Vue 实例、渲染并处理虚拟 DOM 等的代码。基本上就是除去编译器的其它一切。

如果你需要在客户端编译模板 (比如传入一个字符串给 template 选项，或挂载到一个元素上并以其 DOM 内部的 HTML 作为模板)，就将需要加上编译器，即完整版.

当使用 vue-loader 或 vueify 的时候，*.vue 文件内部的模板会在构建时预编译成 JavaScript。你在最终打好的包里实际上是不需要编译器的，所以只用运行时版本即可。



使用原生 `ES Modules`，这里也有一个兼容 ES Module 的构建文件
```
<script type="module">
  import Vue from 'https://cdn.jsdelivr.net/npm/vue@2.6.14/dist/vue.esm.browser.js'
</script>
```

` NPM 包`

 `unpkg` 和 `cdnjs`
 
 # 二、环境
 1. 安装node.js
 2. 常用命令 及基础包
 ```
 npm install -g @vue/cli  #npm update -g @vue/cli 
 vue --version
 vue create hello-world  #vue ui
 
 npm run serve		#运行 vu 项目
 npm run serve -- --port 9000
 
 #在项目中使用依赖
 npm install --save axios vue-axios
 npm install element-plus --save
 
npm install vue-router@4 -s 	#https://unpkg.com/vue-router@4
npm install vuex@next --save	#状态管理
npm install marked -s


#构建生成包
npm run build


npm i typescript ts-loader

 ```
 # 三、vue 基础
 # 1. vue 事件
 |事件|意义|可用的元素|
 |--|--|--|
 |click|单击事件，当组件被单击时触发||
 |dblclick|双击||
 |focus|获取焦点事件|input\select\textarea等|
 |blur|失去焦点|input\select\textarea等|
 |change|元素内容改变，输入框结束输入，内容有变化触发|input\select\textarea等|
 |select|选中，输入框中的文本被选中触发|input\select\textarea等|
 |mousedown|鼠标按下||
 |mouseup|||
 |mousemove|||
 |mouseout|移出组件||
 |mouseover|移入组件||
 |keydown||所有表单元素|
 |keyup||所有表单元素|
 
 ## 1.1 事件修饰符
 |事件修饰符|作用|
 |--|--|
 |stop|阻止事件传递|
 |capture|监听捕获场景的事件|
 |once|只触发一次事件|
 |self|当事件对象的target属性是当前组件时才触发事件|
 |Prevent|禁止默认的事件|
 |passive|不禁止默认的事件|
 |||
 