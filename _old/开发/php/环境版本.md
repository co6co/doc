---
layout: post
title: 环境版本
subtitle:
date:       2022-04-14 13:20:19
categories: [php]
tags: [php,环境版本]
---


# 1. Thread Safe和Non Thread Safe

## 1.1. 两种服务模式ISAPI 和 FastCGI
`SAPI` 服务模式是以 DLL 动态库的形式被调用，可以在被用户请求后执行，在处理完一个用户请求后不会马上消失，所以需要进行线程安全检查，以防止耗尽系统资源；  
`FastCGI` 执行方式则是以单一线程来执行操作，所以不需要进行线程的安全检查，除去线程安全检查反而可以提高执行`效率`。

当 PHP 以 ISAPI 模式运行服务时，选择 Thread Safe(简称ts) 版本，  
以 FastCGI 模式运行服务时，选择 Non Thread Safe(简称nts) 版本。

IIS 以 FastCGI 模式运行服务，只能选择 Non Thread Safe 版本，Apache 以 ISAPI 模式运行服务，只能选择 Thread Safe 版本;
Nginx连接php,配置将.php 结尾的请求通过 `FashCGI` 交给 `PHP-FPM`(FastCGI管理器) 处理。  

可以通过 `phpinfo() ` 函数页面里的 Thread Safety 项查询当前 PHP 版本是不是线程安全的。

# 2. 打开扩展功能

php库缺失 curl  
php库缺失 mb_string  
须开启php GD库,否则验证码、缩略图使用将不正常


1. extension_dir 
 `extension_dir = "F:/Soft/Toolexe/www/php-7.4.28/ext"` //默认会去C:\php 中寻找
2. 打开扩展功能
```
取消下面三行注解
extension=curl
extension=gd2
extension=mbstring
```