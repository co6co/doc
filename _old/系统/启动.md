---
layout: post
title: 启动
subtitle:
date:       2021-08-18 15:36:22
categories: [系统]
tags: [系统,启动]
---


#MBR
#ESP
` EFI/Boot/bootx64.efi→\efi\Microsoft\boot\bootmgfw.efi→efi\Microsoft\Boot\BCD→\Windows\system32\winload.efi→加载内核启动系统`
```
#√  表示不可缺少的文件
√EFI/Boot/bootx64.efi(或bootia32.efi)
√ EFI/Microsoft/Boot/bootmgfw.efi
√ EFI/Microsoft/Boot/BCD
EFI/Microsoft/Boot/zh-CN
```
- 从光盘复制到`ESP`相应位置
- `bcdboot C:\windows /l zh-cn`
  若自行创建的FAT分区（不是标准的ESP分区），则需要执行命令：`bcdboot C:\windows /l zh-cn /s Y:` （Y代表FAT分区盘符）。
  
#mbr+uefi
win7的bcdedit会从以下注册表读取信息，获取引导分区位置`HKLM\BCD00000000`
```
[HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Control]
"FirmwareBootDevice"="multi(0)disk(0)rdisk(0)partition(1)"
"SystemBootDevice"="multi(0)disk(0)rdisk(0)partition(2)"
```

#Bcdedit 
```
bcdedit								#查看「Windows 启动加载器」中的「标识符」和「bootlog」信息
bcdedit /set {current} bootlog Yes	#激活 ntbtlog.txt 日志
//c:\Windows\ntbtlog.txt
```


# 2 系统启动
## 2.0 引导方式
### 2.0.1 EFI
	a.按下电源键，电源向主板以及其它设备供电，刚开始电压不稳，主板会持续发送RESET信号给CPU，CPU执行初始化。
	 当电压稳定后，主板停止发送RESET信号，CPU开始执行指令；
	
	b. CPU读取UEFI指令，执行UEFI初始化，执行其它设备的初始化；
	c. 查找EFI分区，EFI分区不需要一定是第一个分区；
	d. 加载`\EFI\Boot\bootx64.efi`， Windows `\EFI\Microsoft\Boot\bootmgfw.efi`==`\EFI\Boot\bootx64.efi`
	e. bootmgfw.efi会读取`BCD`文件，BCD是一个数据库文件，如果包含多个系统，信息会包含在BCD中，通过显示一个系统列表供用户选择；
	f. BCD中包含每个系统的`引导文件`的路径，Windows的是`\Windows\System32\winload.efi`，加载到内存中并执行；
	g. winload.efi读取`\Windows\bootstat.dat`文件，有需要则显示引导菜单，比如安全引导等等；
	   接着加载内核程序`Ntoskrnl.exe`，相关辅助HAL.dll、CI.dll、PSSHED.dll、BootVID.dll，把CPU执行权交给内核程序；
	h. 内核程序执行系统初始化。
### 2.0.2 BIOS WINDOWS
	a. 上电并稳定后，CPU执行地址`0xFFFF0h`处指令，此处为BIOS程序；
	b. BIOS进行硬件自检，没有问题后加载硬盘的第一个扇区到内存`0x7c00h`处，第一个扇区为`MBR（MasterBoot Record）`，
		MBR包含执行程序和分区表；
	c. CPU开始执行MBR程序，查找第一个活动分区，把活动分区的第一个扇区加载到内存中，活动分区第一个扇区为`PBR（Partition Boot Record）`；
	d. CPU开始执行PBR，第一个指令就是跳过BPB（BIOS Parameter Block）到可执行代码处；
		BPB包含比较多参数，有族的大小、MFT记录大小、MFT位置等，用于读取NTFS文件；

	e. PBR读取`VBR（Volume BootRecord，占用分区开始的16扇区）`剩余的15扇区到内存中；
		接着CPU跳转到0x07C0:027A处，执行BOOTMGR代码（第二个扇区中）；

	f. 开始寻找`bootmgr.exe`，找不到则寻找`ntldr.exe`（win vista之前的系统）；
	g. CPU加载并跳转到`bootmgr.exe`处执行，读取`BCD`文件，如果含有多个系统，则列举显示供用户选择；
	h. 选择的是Windows则读取`winload.exe`文件到内存中，CPU跳转到winload.exe处执行，
		读取文件`\windows\bootstat.dat`，，有需要则显示引导菜单，比如安全引导等等；
		接着加载内核程序`Ntoskrnl.exe`，相关辅助HAL.dll、CI.dll、PSSHED.dll、BootVID.dll，把CPU执行权交给内核程序；

	m.内核程序执行系统初始化。

###  2.0.3 BIOS LINUX 
	a.  开机 ，计算机相对较笨，只能读取部分存储空间（ROM），由于ROM的特性，这时CPU处于只读模式。
		在ROM中，存储了一个称为固件的小程序（BIOS），它进行加电自检，检测硬件等（POST），
		最主要的是，允许访问其他类型的内存（硬盘或RAM）。
		BIOS存储了磁盘的启动顺序，BIOS按照顺序去找MBR。
		找到后会将`MBR中的引导代码`复制到RAM中（是一个446字节的`boot.img`镜像）。
		并转移控制权(CPU)。MBR本身还包含一个`64字节的分区表`，描述了分区的大小和其他属性。
		剩余的2字节用于磁盘签名。
	b. 由于MBR引导程序不能理解文件系统的结构，因此它必须定位`core.img`然后加载到RAM中，并转移控制权。
		core.img代码必须位于MBR和第一个分区之间的位置，
		第一个分区的扇区`开始位置是63`，可以计算（一个扇区为512bytes），62*512=31744，
		然后减去MBR占用的一个扇区31744-512=31232，
		而一个core.img的大小也就26676左右。
		还有一个`diskboot.img`，它是core.img的第一个扇区（即整个磁盘的第二个扇区），
		其唯一目的是加载core.img中通用的文件系统的驱动，例如FAT,NTFS,EXT等。，
		这时就可以直接访问文件系统了（/boot文件）。 
	c. core.img进入32位保护模式，然后加载`/boot/grub2/i386-pc/normal.mod`，
		normal.mod用于解析/boot/grub2/grub.cfg文件，可选择加载模块（例如，用于图形UI）并显示菜单。
		然后通过用户的选择（或者默认）将指定的内核加载到RAM中。并将控制权交给内核
	d. 内核同通常以镜像文件的形式加载，用zlib压缩成`zImage或bzImage格式`，内核作为压缩的镜像文件，将解压缩自身。
	e. 内核会立即初始化和配置计算机内存，并配置附加到系统上的各种硬件，包括所有处理器，I/O子系统和存储设备。
		然后它在内存中预定的位置查找压缩后的`initrd（临时根文件系统）`，对其进行解压，
		然后内核启动虚拟内存交换器（它是一个内核进程）并创建一个根分区，以只读方式挂载，
		该initrd中包含各种可执行程序和驱动程序。然后内核创建初始用户空间进程执行`/sbin/init`，
		这里的init是/usr/lib/systemd的链接文件（即调用systemd，其PID为1）。
		等待systemd将实际的根文件系统挂载后，再将initrd这个临时的根文件系统卸载。
	f. systemd读取`/etc/fstab`文件中的配置进行挂载。
		再根据运行级别(通过`/etc/systemd/system/default.target`链接文件得知运行级别)
		然后启动该运行级别的一系列的依赖（
		详细的启动顺序可使用命令`systemd-analyze plot > boot.html`然后用浏览器查看）。
		最后执行`getty.target`启动终端和登陆程序。至此，整个过程结束。

## 2.1 windows
### 2.1.1 XP
1. 预引导阶段
	Power-On Self Test，POST 处理器、内存等硬件 定位引导设备-->读取并运行主引导记录（Master Boot Record，MBR）
2. 引导阶段
	初始化引导载入程序、操作系统选择、硬件检测、硬件配置文件(使用到的文件：Ntldr、Boot.ini、Ntdetect.com、Ntoskrnl.exe、Ntbootdd.sys、Bootsect.dos（非必须）)
	a. ntldr :处理器由实模式（Real Mode）切换为32位平坦内存模式（32-bit Flat Memory Mode）
	   ntldr读取NTFS驱动（系统自带）找到硬盘分区
	b.`ntldr--读取->boot.ini`（Windows 2000/XP/2003  保存在`boot.ini`），判断 显示列表
	c.硬件检测Ntdetect.comu收集硬件信息--给->Ntldr（创建注册表中有关硬件的键）
	d.配置文件选择 (分别在办公室和家里使用不同的硬件配置文件)
	e.Ntldr 载入Windows XP 内核文件`Ntoskrnl.exe`(仅是载入未.初始化)， 载入硬件抽象层`hal.dll`。
	f.载入`HKEY_LOCAL_MACHINE/System` ,Ntldr 根据载入的Select键的内容--判断-->Control Set注册表键(ControlSet001--ControlSet003)
	  将进入初始化内核阶段-->ntldr会将系统的控制权交给操作系统内核
3. 初始化内核阶段
	该阶段主要完成创建Hardware注册表键、对Control Set注册表键进行复制、载入和初始化设备驱动，以及启动服务。
	a. 显示Windows XP的标志 出现滚动条
	b. 创建Hardware键，使用前收集到的硬件信息来创建`HKEY_LOCAL_MACHINE/Hardware`键
	c. 复制Control Set注册表键（用于“最后一次正确配置” ）
	d. 载入和初始化设备驱动
	   载入的底层设备驱动
	   内核 查找`HKEY_LOCAL_MACHINE/System/CurrentControlSet/Services` 所有Start键值为“1” 的设备驱动，这些设备驱动会在载入之后立刻进行初始化
	   发生了任何错误，自动根据设备驱动的“ErrorControl”键的数值进行处理
	   ```
	    0，忽略，继续引导，不显示错误信息。
		1，正常，继续引导，显示错误信息。
		2，恢复，停止引导，使用“最后一次正确配置”选项重启动系统。如果依然出错则会忽略该错误。
		3，严重，停止引导，使用“最后一次正确配置”选项重启动系统。如果依然出错则会停止引导，并显示一条错误信息
	   ```
	   至此完成内核载入。
	e. 启动服务
	  会话管理器--启动->高层子系统和服务
	  启动Win32子系统（控制所有输入/输出设备以及访问显示设备）
	  Windows的图形界面显示出来，可以使用键盘以及其他I/O设备
	  会话管理器启动的winlogon.exe进程将会启动本地安全性授权（Local Security Authority，lsass.exe）子系统
	  
	  登录系统
	  
	  `HKEY_LOCAL_MACHINE/System/CurrentControlSet/Services`搜索Start“2”服务,用户成功登录之后才开始加载这些服务
	  
	

