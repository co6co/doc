---
layout: post
title: git 命令
subtitle:
description:
header-img:
date: 2024-4-22 10:50:01
categories: [工具]
tags: [命令行, git]
---

# 跟踪

本地分支与远程分支的关联 remote tracking
克隆是出现：

```
local branch "main" set to track remote branch "o/main"

# 实际使用中用：
git push --set-upstream origin brachName
```

## 方法 1

```
git checkout -b localBratch o/main # 创建一个名为 localBratch 的分支，它跟踪远程分支 o/main
git checkout -b foo o/main ; git pull;
git checkout -b foo o/main ;git commit;git push;
```

## 方法 2

```
git branch -u o/main foo
# 当前分支为 foo时 可以写为： git branch -u o/main
git branch -u o/main foo; git commit;git push;
```

# pull

```
git pull origin foo == git fetch origin foo;git merge o/foo # 合并 o/foo 到当前分支

```

# push

```
git push <remote> <place>  # place == <source>:<destination>

git push origin main # 切到本地仓库中的“main”分支，获取所有的提交，再到远程仓库“origin”中找到“main”分支，将远程仓库中没有的提交记录都添加上去
git push origin <source>:<destination>

git push origin :side # 删除远程分支 side
git fetch origin :side # 创建本地分支 side
```
