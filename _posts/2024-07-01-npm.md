---
layout: post
title:  npm 上传包
subtitle: npm 上传包
description: npm 上传包

header-img: 
date:       2024-07-01 14:19:01
---
# 1. `npm run dev` //跑一下，确定没问题再上传。
# 2. NPM 发布包
## 2.1 登录
```
npm login 
npm who am i # 查看是否登录

```
## 2.2 发布
打包前忽略的文件，源文件一般在 lib 或 src 目录下，但生成的文件一般在 dist 或 build 目录下，
而我们会把这个构建目录在 .gitignore 中给忽略掉，导致发包之后也看不到。
1. 增加 `.npmignore `文件，把忽略的文件写在这里，因为默认情况下，npm 打包会根据 `.gitignore `来忽略文件，如果发现存在 `.npmignore` 的话，则使用这个文件。
2. 在 `package.json` 中增加 files 数组，把需要打包的文件写在这里，那么 npm 打包的时候只会按照你列出来的规则添加文件。它的优先级高于 `.npmignore` 和 `.gitignore`，且支持通配符。

常用发布方法： 
只想把 dist 目录生成的文件打包上传，并不想提供源代码，或者我们是用 TypeScript 写的，发包的时候只发编译过的 JavaScript 文件和 d.ts 声明文件。此时只需要把 package.json 拷贝一份到 dist 目录，然后在该目录下运行 npm publish 即可。
为了防止搞混且误提交，可以在源代码中的 package.json 添加 private: true 字段，而在 dist 的 package.json 中去掉该字段。
 
```

//切换到npm地址
//可能使用到的命令
npm config set registry=https://registry.npmjs.org
npm config edit #打开文本文件编辑
 
registry=https://registry.npmmirror.com

# registry 切换 
# nrm 为 registry 切换工具
npm install -g nrm 
nrm use taobao
nrm use npm

# 发布前 检查那些文件会被发布出去 或者本地打个包看看有那些文件
npm publish --dry-run # 查看有文件
npm pack              # 打个包看看有那些文件

//因为上传私有组件需要付费，所以上传为公共的
npm publish --access public 


```
## 3 其他相关操作

```
# 查看远程是否存在包名
npm view 包名

# 配置初始化
npm init
npm init -y   # ts 项目使用  tsc --init
# 删除
npm unpublish co6co --force #删除库包之后，24小时之内 无法再次上传同名的库包到 npm，需要更改包名，或者等过了 24小时之后 在上传。
npm unpublish co6co@0.0.1 --force #你只能撤回在最后 72 小时内发布的版本
npm unpublish --force # 删除 package.json 的包名相应的版本

# 废弃 安装时会有警示，并不影响使用。
npm deprecate <pkg>[@<version>] <message> 

```
# 4. 使用
```
npm install xxx
```
# 5. 其他相关
```

添加 "declaration": true 到你的 tsconfig.json --> 告诉 TypsScript 在编译的时候为你自动生成 d.ts 文
添加 "types": "index.d.ts" 到你的 package.json --> 导入包时，告诉了 TS 编译器到哪里去寻找类型定义文件

```

## 5.1 package.json版本说明
	a.  确切版本:
		当你指定一个确切的版本号，例如 "lodash": "4.17.21"，npm 将会安装这个确切的版本。
	b. 波浪线 ~:
		当你使用波浪线前缀，例如 "lodash": "~4.17.21"，npm 将会安装指定的主要版本和次要版本，但会允许修复补丁级别的更新。也就是说，它会安装 4.17.x 的任何版本，其中 x 是任何大于等于 21 的数字。
	c.	尖括号 ^:
		当你使用尖括号前缀，例如 "lodash": "^4.17.21"，npm 将会安装指定的主要版本，但允许次要版本和修复补丁级别的更新。也就是说，它会安装 4.x.x 的任何版本，其中 x 是任何大于等于 17 的数字。
	d. 大于或小于 > <:
		使用大于或小于符号可以指定一个版本的界限。例如，">4.17.21" 将会安装任何大于 4.17.21 的版本，而 "<=4.17.21" 将会安装任何小于等于 4.17.21 的版本。
	e. 通配符 *:
		使用星号作为版本标识符，例如 "lodash": "*"，将会安装最新版本，但是这种方式并不推荐，因为它可能导致不稳定的依赖关系。
	f. 多个范围:
		你可以指定多个版本范围，用逗号分隔，例如 "lodash": ">=4.17.21 <5.0.0"，这将安装 4.17.21 或更高版本，但不包括任何 5.0.0 或更高版本。
	g. 排除范围:
		使用 ! 或 not 关键词可以排除某些版本，例如 "lodash": "^4.17.21", "!4.17.25" 将会安装 4.x.x 的任何版本，除了 4.17.25。