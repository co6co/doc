---
layout: post
title:  Abd工具 

header-img: 
date:   2024-12-12 16:30:01
modify: 2025-01-14 16:55:00
categories: [工具]
tags: [abd]
---

# 密码文件

## a. 手势密码
九个点的数据含义，含义是这样的：
从左上角起编号为 00，至右下角止编号为 08。那么“闪电”的编码应该是01030406。
使用`SHA1算法`对上述十六进制编码进行计算后得到密文“F2A5B79ECD8B5428F474355287BE6ED857E73208”
```
adb shell到设备上
adb pull /data/system/gesture.key # 下载到本地
rm  /data/system/gesture.key	  # 删除密码文件 	
``` 

## b. 键盘密码
4-16位字符密码[94个字符，包括52个大小写英文字母+10个数字+30个除空格外的标点符号]
加入salt后进行SHA1和MD5散列值计算,暴力破解基本不可能
```
rm  /data/system/password.key
```

# adb 一些命令
需要允许USB调试模式弹出框
## a. 重启adb
输入命令：`adb devices`。显示`unauthorized `,可以重启adb server或者重新获取授权
```
# 重启
adb kill-server
adb start-server 
adb shell

#以尝试重新授权
cd /Users/用户名/.android/
rm adbkey adb_usb.ini
```

#　adb 命令
```
adb shell	#启动 background 终端
exit
adb devices		 #显示连接到计算机的设备列表
adb reboot
adb sideload su-package.zip #ideload一个更新包 ，su更新包可以是SuperSu、SuperUser
adb reboot recovery
adb reboot download #将连接的设备重新启动到下载模式。例如，三星 Galaxy 设备上的下载模式。
adb reboot bootloader #将设备重启到 Bootloader 中
adb reboot fastboot #重新启动到 Fastboot 模式
adb install <ApplicationpathPackagename>.apk
adb install -r <AplicationpathPackagename>.apk #已经安装了一个应用程序，并且您只想更新它
adb unistall package_name
adb uninstall -K package_namee.g #卸载应用程序，但保留其 data 和 cache 目录
adb push c:\users\UsamaM\desktop\Song.mp3 \system\media
adb pull \system\media\Song.mp C:\users\UsamaM\desktop
adb pull /system/app backup/systemapps #所有系统应用程序备份指定目录 

```

# fastboot
```
Fastboot Flash File.zip   #将 .zip 文件刷入您的手机
fastboot devices #列出所有通过 USB 连接并且处于 fastboot 模式的设备。
fastboot reboot #重启设备进入正常模式。
fastboot reboot-bootloader #重启设备进入 bootloader 模式（即 fastboot 模式）。
fastboot flash [partition] [filename] #将指定文件刷写到设备的特定分区。例如：
fastboot flash boot boot.img #- 刷写新的引导镜像。
fastboot flash system system.img #- 刷写系统分区。
fastboot flash recovery recovery.img #- 刷写恢复镜像。
fastboot erase [partition] #擦除指定分区。例如：fastboot erase system 会擦除系统分区的所有数据。
fastboot oem divice-info 	# 查看设备状态
fastboot oem unlock #解锁设备的引导加载程序。这通常会导致设备上的所有用户数据被清除，并且可能会使设备失去保修资格。
fastboot oem lock #锁定设备的引导加载程序。锁定后，你将无法再刷写自定义镜像或解锁引导加载程序，除非再次解锁。
fastboot getvar [variable] #获取设备的变量信息 
fastboot getvar all - 获取所有可用的变量信息。
fastboot getvar version - 获取 fastboot 协议版本。
fastboot continue #在某些情况下，如果设备在 fastboot 模式中挂起，可以使用此命令让设备继续启动过程。
fastboot set_active [a|b] #对于支持 A/B 分区的设备，设置当前活动的系统分区为 A 或 B。这对于无缝更新非常重要。
fastboot --set-active=[a|b] #另一种方式来设置活动分区，适用于较新的 Android 版本。
fastboot flashing [unlock|lock] #有些设备可能需要使用 flashing unlock 或 flashing lock 来代替 oem unlock 和 oem lock。
fastboot flash:raw [command] #用于执行原始分区的刷写操作，这个命令在较老的设备上更为常见。
fastboot update [filename] #使用完整的 OTA 包或官方固件包更新设备。
Fastboot getvar cid	#显示您手机的 CID。
Fastboot oem writeCID xxxxx	#writes the super CID.

#帮助您获取手机的标识符令牌，该令牌可用于解锁 bootloader。
#第二个命令可帮助您刷写引导加载程序解锁代码，
#第三个命令可帮助您再次锁定手机的引导加载程序
fastboot oem get_unlock_data
fastboot oem unlock UNIQUE_KEY
fastboot oem get_identifier_token
fastboot oem flash Unlock_code.bin
fastboot oem lock

#这个和手机上现有的系统完全无关，只要本地的 recovery.img是以前能正常进rec的，那就绝对没问题。
#那些官升或者刷rom后无法进入rec的其实都可以通过这种方式进入recovery 环境进行刷机、刷rec，刷各种zip
fastboot boot boot.img  #用当前目录下的boot.img启动手机，在手机boot分区损坏的情况下可以用这个正常进入系统
fastboot boot recovery.img  #用当前目录下的recovery.img启动手机到recovery模式，

```

```
1.手机开机状态，确定adb可以打开。
 
2.输入命令 adb reboot bootloader
 
fastboot模式下 常用命令大全
 
fastboot常用命令：
 
1、adb reboot bootloader      进入fastboot 模式
2、fastboot flashing unlock    解锁unlock状态 
3、fastboot flashing lock        锁定状态 
4、fastboot erase data           清空data数据 
5、fastboot format data        格式化data分区数据 
6、fastboot getvar devices-state     查看锁的状态 
7、fastboot erase boot       擦除boot分区 
8、fastboot flash boot **.img      烧写img 
9、fastboot reboot      重启 
10、fastboot oem reboot- 重启到相应模式 
11、fastboot oem device-info 查看解锁状态 
12、fastboot oem unlock 解OEM锁 
fastboot flash partition gpt.bin 注释：刷写gpt分区表 
fastboot flash bootloader bootloader.img 注释：刷写bootloader引导文件 
fastboot flash modem NON-HLOS.bin 注释：刷写基带 
fastboot flash fsg fsg.mbn 注释：刷写射频表 
fastboot erase modemst1 注释：清除基带缓存 
fastboot erase modemst2 注释：清除efs射频缓存 
fastboot flash dsp adspso.bin 注释：刷入dsp底层文件 
fastboot flash logo logo.bin 注释：刷入开机logo 
fastboot flash boot boot.img 注释：刷写boot分区，主要包含系统内核及挂在其他分区的ramdisk 
fastboot flash recovery recovery.img 注释：刷入recovery镜像 
fastboot flash system system.img_sparsechunk.0 注释：刷入system分区，主要包含Android的系统应用程序和库文件 
fastboot flash system system.img_sparsechunk.1 
fastboot flash system system.img_sparsechunk.2 
fastboot flash system system.img_sparsechunk.3 
fastboot flash system system.img_sparsechunk.4 
fastboot flash system system.img_sparsechunk.5 
fastboot flash system system.img_sparsechunk.6 
fastboot flash system system.img_sparsechunk.7
fastboot flash system system.img_sparsechunk.8 
fastboot flash system system.img_sparsechunk.9 
fastboot flash system system.img_sparsechunk.10fastboot flash system system.img_sparsechunk.11 
fastboot flash oem oem.img_sparsechunk.0 注释：刷入oem文件 
fastboot flash oem oem.img_sparsechunk.1 
fastboot flash oem oem.img_sparsechunk.2 
fastboot erase carrier 注释：清除carrier 
fastboot erase cache 注释：清除缓存(非必选) 
fastboot erase userdata 注释：清除用户数据(非必选) 
fastboot erase ddr 注释：清除ddr分区(非必选) 
fastboot oem fb_mode_clear 注释：清理fastboot模式缓存
```
## 1. 刷入 recovery
```
.\fastboot flash recovery twrp-3.4.0-0-davinci.img
```
## 2. 如果不希望安装 TWRP，而只是临时使用 TWRP，运行下面命令（自行替换文件名）并忽略第 3 步骤。
```
.\fastboot boot twrp-3.4.0-0-davinci.img
```
3. 为防止重启手机 MIUI 自动替换回官方 Recovery，按住手机音量上键，同时电脑运行下面命令重启手机，直至进入 TWRP 界面。

.\fastboot reboot
4. 在进入 TWRP 后，会询问“是否保持系统分区为只读”。这里滑动按钮以允许修改 ，不然不能禁止 MIUI 替换回官方 Recovery。
由于 MIUI 在启动时会检查 System 分区完整性，上一步 TWRP 修改了 System 分区，这时重启手机会无法启动系统（“卡米”问题），需要通过刷入 Magisk 移除启动验证，步骤如下。
电脑下载 Magisk 安装包，将其复制到手机上（这时电脑可以识别手机 MTP 设备）；
TWRP 界面上点击“安装”，找到下载的是 Magisk 安装包，点击文件名，滑动按钮刷入；
等待 Magisk 刷入完成，点击“重启系统”，这时就不会有“卡米”问题，重启手机也不会丢失 TWRP 安装；


## HTC 官网解锁方法
1. `fastboot oem get_identifier_token` 显示一个令牌（token）将其提交到HTC的解锁门户网站
2. 用户会接收到一个为其手机定制的`Unlock_code.bin`文件，这个文件由`HTC的私钥签名`，应该使用`fastboot flash unlocktoken Unlock_code.bin`命令将其刷入设备中。
3. 如果Unlock_code.bin文件是有效的，手机将允许使用标准的`fastboot flash`命令，来刷入未经签名的分区镜像
## 其他一些加锁方案
东芝的某些款平板电脑，也有`NAND锁`。对于这些设备，锁是由`sealime`可加载内核模块来实施的，而这一模块存在于引导镜像`initrd`中。
这个模块以`SEAndroid`为基础，防止重新挂载系统分区进行写操作。

# root
- root的set-uid或set-gid二进制文件在Android官方版本中并不常见，运营商或设备制造商有时会在他们的定制修改版本中引入一些；
- adbd守护进程是以root权限开始运行的，然后会降权至shell用户（AID_SHELL），除非属性ro.secure被设置为0。然而这一属性是只读的，通常情况下由引导镜像initrd设置成ro.secure=1
- 某些品牌的安全非易失随机存取存储器（NVRAM）中有一个安全标志位（@secuflag），这一`标志位`会被设备的引导加载程序（HBOOT）检查。当这个标志位设置为true时，引导加载程序会显示一
条`“security on”`消息（`S-ON`），而NAND锁会被强制执行。NAND锁防止对系统、启动和恢复分区进行写入，而启用S-ON模式后，重新启动将失去root权限，并且对于这些分区的写入不会持久化。这使得定制系统ROM、定制内核和定制恢复修改都不再可行。
通过对一个足够严重的安全漏洞的利用获取到root权限，然而NAND锁使得所有的修改在重启之后都会丢失，这在Android MOD（修改）社区中被称为`临时性root`。
对开启NAND锁的HTC设备进行`永久性root`，必须完成以下两件事中的一件。
- 一是禁用基带上的安全标志位；
- 二是将设备刷上一个不会实施NAND锁的打补丁的HBOOT或工程HBOOT。

在两种情况下，引导加载程序都会显示一条“security off”消息（S-OFF）。

## 获取永久root
方法1：
拥有一个root shell（软root），要实现永久的root访问权限是非常简单的。在没有开启NAND锁的手机上，你只需要对系统分区的写权限。如果手机有NAND锁，那么首先需要移除掉它
NAND锁被移除之后，你就可以简单地以读写模式重新挂载系统分区，放置一个设置set-uid root权限的su二进制程序，然后再次以只读模式挂载。当然，你也可以选择安装一个su程序的包装应用，如
SuperUser或SuperSU。
```
adb shell mount -o remount,rw /system
adb adb push su systemxbin/su
adb shell chown 0.0 systemxbin/su
adb shell chmod 06755 systemxbin/su
adb shell mount -o remount,ro /system
adb install Superuser.apk

```
方法2：
将定制恢复镜像写入恢复分区中，可以在Android设备上使用dd命令做到这点。这相当于通过fastboot或下载模式刷入一个定制的恢复镜像，
首先，你需要确定设备上恢复分区的位置：
```
shell@android:/ # ls -l devblock/platform/*/by-name/recovery
lrwxrwxrwx root root 2012-11-20 14:53 recovery -> devblock/mmcblk0p7
## 你可以将一个定制恢复镜像复制到SD卡上，然后写入恢复分区：
adb shell push custom-recovery.img sdcard
adb shell dd if=sdcardcustom-recovery.img of=devblock/mmcblk0p7
#需要重新启动，进入定制恢复模式，并应用su更新包
adb reboot recovery


```

