---
layout: post
title: OMV（OpenMediaVault）

header-img:
date: 2026-05-25 10:15:01
modify: 2026-05-25 10:15:01
categories: [nas, 存储]
tags: [NAS, OpenMediaVault, OMV]
---

# 1. 第三方 源 [OMV-Extras]

## github 源

wget -O - https://github.com/OpenMediaVault-Plugin-Developers/packages/raw/master/install | bash

## 国内源

wget -O - https://cdn.jsdelivr.net/gh/OpenMediaVault-Plugin-Developers/installScript@master/install | sudo bash

## 勾选 pack

# 2 插件 中搜索 openmediavault-compose 并安装

OMV7 把老版本 的 docker / portainer / compose 三个合为 openmediavault-compose

## 应用后报签名错误

```
//1. # 设 Docker 源 URL（清华）
sudo omv-env set OMV_DOCKER_APT_REPOSITORY_URL "https://mirrors.tuna.tsinghua.edu.cn/docker-ce/linux/debian"

//2. # 让 env 生效（会 regenerate 一堆 salt 模板，包括 omvdocker.sources）
sudo omv-salt stage run all

//3. # 再清一下 apt 缓存，防止旧 list 残留
sudo omv-aptclean repos

//4. # 最后重新部署 compose
sudo omv-salt deploy run compose
```

# 3 安装 openmediavault-webdav

插件中搜索 openmediavault-webdav

- 先建共享文件夹（比如叫 webdav-share）
- 给 www-data 用户和 webdav-users 组 rw 权限（ACL 里也要给，不然 403）
- 服务 → WebDAV 启用，共享文件夹选刚建的，用户组选 webdav-users
- 把你的用户加到 webdav-users 组
- 访问地址：http://OMV的IP/

# 4. centos 挂载

## 4.1 CentOS 7

sudo yum install -y epel-release
sudo yum install -y davfs2

```

编辑 /etc/davfs2/davfs2.conf
use_locks 0                  # Nginx WebDAV 不实现 LOCK，必关
ignore_dav_header 1           # Nginx 的 DAV 头不完整
buf_size 16384                # 1.5+ 单位是字节，16384 = 16KiB（1.4.7 时是 128 KiB，别抄错）
dir_refresh 30
file_refresh 10
# 1. secrets 免密

sudo vim /etc/davfs2/secrets
# 末尾加（换成你的 OMV IP / 账号 / 密码）：
http://192.168.1.10/webdav   your_omv_user   your_omv_password

sudo chmod 600 /etc/davfs2/secrets

# 2. fstab
sudo vim /etc/fstab
# 加一行（_netdev 等网络起来再挂，CentOS 7 老 init 也认）：
http://192.168.1.10/webdav   /mnt/omv-webdav   davfs   rw,user,_netdev,uid=$USER,gid=$USER   0   0

# 3. 建挂载点 + 试挂
sudo mkdir -p /mnt/omv-webdav
sudo mount -a
ls /mnt/omv-webdav
可能用到
sudo umount /mnt/omv-webdav
```

## 4.2 挂载在win

showmount -e 192.168.1.115 #查看目录名
mount \\192.168.1.115\export\nfs Z:
umount Z:

# 5. python 应用

## 5.1 安装虚拟环境

```
sudo apt update
sudo apt install -y python3-venv python3-pip
```

## 5.2 创建虚拟环境

```
mkdir -p ~/envs/myapp
cd ~/envs/myapp
python3 -m venv .venv
source .venv/bin/activate  #激活

(.venv) user@omv:~/myapp$
which python
python --version
deactivate

```

# 6. nats-server

## 6.1 复制文件到指定目录，并给权限

```
sudo chmod +x /usr/local/bin/nats-server
```

## 6.2 使用专门用户来跑

```
sudo useradd -r -s /bin/false -d /var/lib/nats -M nats
sudo mkdir -p /var/lib/nats /var/log/nats
sudo chown -R nats:nats /var/lib/nats /var/log/nats
```

## 6.3 最小配置

```
sudo mkdir -p /etc/nats
sudo nano /etc/nats/nats.conf
```

💡 4222 = 客户端口（Python nats-py 连这个），8222 = HTTP 监控（curl localhost:8222/health 能看到状态）。

```
# /etc/nats/nats.conf
port: 4222
http_port: 8222

listen: 0.0.0.0

debug: false
trace: false

log_file: "/var/log/nats/nats.log"
log_size_limit: 50MB

# 如果需要用户名密码，把下面打开
# authorization {
#   user: "nats"
#   password: "换一个强密码"
# }
```

## 6.4 配置服务

GNU nano 8.4 /etc/systemd/system/nats-server.service

```
[Unit]
Description=NATS Server
Documentation=https://docs.nats.io
After=network-online.target

[Service]
Type=simple
User=nats
Group=nats
ExecStart=/usr/local/bin/nats-server -c /etc/nats/nats.conf
ExecReload=/bin/kill -s HUP $MAINPID
Restart=on-failure
RestartSec=5s

# Lame Duck Mode
KillSignal=SIGUSR2
TimeoutStopSec=30

[Install]
WantedBy=multi-user.target
```

## 6.5 开机启动

```
sudo systemctl daemon-reload
sudo systemctl enable --now nats-server
sudo systemctl status nats-server
```

## 6.6 测试是否成功

```
nats-server --version
ss -lntp | grep 4222

# HTTP 监控
curl http://localhost:8222/health
# {"status":"ok"}

# 控制台运行
# 方法一
sudo journalctl -u nats-server --no-pager -n 50
# 方法二
sudo -u nats /usr/local/bin/nats-server -c /etc/nats/nats.conf


```
